# Copy to baokv.env (git-ignored), fill in, then: docker run --env-file baokv.env ...
# No quotes around values — docker --env-file takes them literally.
BAO_ADDR=https://foobar.example.com
BAO_TOKEN=REPLACE_WITH_YOUR_OPENBAO_TOKEN
BAO_COOKIE=REPLACE_WITH_YOUR_oauth2_proxy_COOKIE_VALUE
